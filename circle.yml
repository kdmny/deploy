dependencies:
  override:
    - yarn

machine:
  environment:
    # For yarn
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

    LOG_LEVEL: silly
    NODE_ENV: test
    PORT: 3000

  node:
    version: "8.1.4"

  timezone: America/Los_Angeles

test:
  override:
    - yarn test
